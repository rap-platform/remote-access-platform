---
description: Remote Desktop Architecture & Coding Standards Rules
globs: **/*
---

# Remote Access Platform - Agent Rules

Refer to `docs/REMOTE-DESKTOP-ARCHITECTURE.md` (v4.0) and `AGENT_RULES.md`.

- **MANDATORY LIVE DOCUMENTATION LOG & PLAN SYNC**: Maintain `docs/IMPLEMENTATION_LOG.md` and sync `docs/architecture_and_implementation_plan.md` whenever milestones, features, or scripts are completed or added.
- **QUALITY GATE & SCRIPT AUTOMATION (`tools/`)**: Run static analysis (`cppcheck`, `clang-tidy`, `clippy`, `rustfmt`), compile, and run tests (`ctest`, `cargo test`) before any build artifact is ready (`tools/build.sh`).
- **Backend**: Rust ONLY (`services/`). `#![forbid(unsafe_code)]` mandatory.
- **Client UI**: Qt 6 (LGPLv3 dynamic linking) + QML (`apps/client/`).
- **Core**: C++20 (`apps/client/src/core/`, `apps/agent/`).
- **Agent**: Qt-free headless service.
- **Cross-Platform**: Windows, Linux, macOS. OS code isolated in `platform/<os>/`.
- **QML Standards**: `id` first, relative sizing, `Palette.*` colors, zero business logic.
- **Logging**: `QLoggingCategory` (C++) and `tracing` (Rust). No secrets in logs.
- **Commits**: Conventional Commits standard.
